<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1 class='text-center'>User Profile</h1>
    <br />
    <div class="container">
      <div class="card mb-3 bg-light border-primary border-left-0">
        <div class="row no-gutters">
          <div class="col-2 col-sm-1 m-auto text-center">
            <i class="fas fa-user fa-3x"></i>
          </div>
          <div class="col-10 col-sm-11">
            <div class="card-body">
              <p class="card-text"><strong>First Name : </strong><%= @user.first_name %></p>
              <p class="card-text"><strong>Last Name : </strong><%= @user.last_name %></p>
              <p class="card-text"><strong>Description : </strong><%= @user.description %></p>
              <p class="card-text"><strong>Age : </strong><%= @user.age %></p>
              <p class="card-text"><strong>City : </strong><%= @user.city.city_name %></p>
            </div>
          </div>
        </div>
      </div>
      <p><%= link_to "Ville de l'Auteur", city_path(@user.city_id), :class => 'btn btn-primary' %> <%= link_to "Retourner à la liste des gossips", gossips_path, :class => 'btn btn-success' %></p>
    </div>
  </body>
</html>
